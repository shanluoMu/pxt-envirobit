namespace DFRobot_PN532{
    /*是否检测到卡片*/
    //% weight = 60
    //% blockId=isDetection block="检测到卡片？"  
    export function IsDetection(): boolean {
        if (1)
            return true
        else
            return false
    }
}